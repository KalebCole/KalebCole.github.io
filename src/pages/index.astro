---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import SiteNav from '../components/SiteNav.astro';
import Polaroid from '../components/Polaroid.astro';
import PinnedRepos from '../components/PinnedRepos.astro';
import RecommendCard from '../components/RecommendCard.astro';
import { getPublishedRecommends } from '../lib/recommends';

const allPosts = await getCollection('blog', ({ data }) => !data.draft);
const recentPosts = allPosts
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const RECENT_RECOMMENDS_COUNT = 3;
const recentRecommends = (await getPublishedRecommends()).slice(0, RECENT_RECOMMENDS_COUNT);
---
<Layout title="Kaleb Cole">
  <main class="wrap">
    <SiteNav />

    <h1>Kaleb Cole</h1>
    <p class="muted">to be determined, writing this manually so it doesn't sound like AI</p>

    <Polaroid />
    <p>
      to be determined, writing this manually so it doesn't sound like AI
    </p>

    <h2>Recent writing</h2>
    {recentPosts.map((post) => (
      <article class="post">
        <h3 class="post-title">
          <a href={`/blog/${post.id}`}>{post.data.title}</a>
        </h3>
        <p class="post-meta">
          {post.data.date.toLocaleDateString('en-US', {
            year: 'numeric',
            month: 'short',
            day: 'numeric',
            timeZone: 'UTC',
          })}
        </p>
        <p class="post-desc">{post.data.description}</p>
      </article>
    ))}
    <p style="margin-top: 1rem;"><a href="/blog">All posts →</a></p>

    <h2>Recent recommendations</h2>
    {recentRecommends.length > 0
      ? recentRecommends.map((item) => <RecommendCard item={item} />)
      : <p class="muted">Nothing here yet. Check back soon.</p>
    }
    <p style="margin-top: 1rem;"><a href="/recommends">All recommendations →</a></p>

    <h2>Projects</h2>
    <PinnedRepos />

    <div class="footer">
      <a href="https://github.com/KalebCole">GitHub</a>
      <a href="https://linkedin.com/in/kaleb-cole">LinkedIn</a>
      <a href="mailto:kalebcole2021@gmail.com">Email</a>
    </div>
  </main>
</Layout>